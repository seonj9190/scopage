# Copy this file to .env.local and fill in real values.
# .env.local is gitignored (matches *.local) — never commit real DB credentials.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=scopage
DB_PASSWORD=change-me
DB_NAME=scopage
